DATABASE_URL="postgresql://tahkeem:tahkeem@localhost:5433/tahkeem?schema=public"
PORT=3001
CORS_ORIGINS="http://localhost:5173,http://localhost:8081"

# Change these before any deployment.
JWT_SECRET="change-me-in-production-a-long-random-string"
JWT_ADMIN_TTL="12h"
# Judge tokens are short-lived by design; the QR itself carries the longer TTL.
JWT_JUDGE_TTL="8h"

# Seeded administrator.
SEED_ADMIN_EMAIL="admin@omar-quran.tn"
SEED_ADMIN_PASSWORD="Admin@2026"
