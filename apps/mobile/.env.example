# Base URL of the tahkeem API.
# On a simulator/emulator "localhost" reaches the host machine.
# On a PHYSICAL device you MUST use the machine's LAN IP instead, e.g.
#   EXPO_PUBLIC_API_URL=http://192.168.1.20:3001/api
EXPO_PUBLIC_API_URL=http://localhost:3001/api
